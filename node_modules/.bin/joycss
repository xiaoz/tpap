#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../joycss/bin/joycss" "$@"
  ret=$?
else 
  node  "$basedir/../joycss/bin/joycss" "$@"
  ret=$?
fi
exit $ret
